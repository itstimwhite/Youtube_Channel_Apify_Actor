{
    "title": "YouTube Channel Scraper input",
    "description": "This actor takes channels as an input and returns their details.",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "startUrls": {
            "title": "Direct URLs",
            "type": "array",
            "description": "Provide some direct Youtube urls, like channel url.",
            "default": [],
            "editor": "requestListSources",
            "sectionCaption": "Direct URLs"
        },
    },
    "required": [
        "input"
    ]
}