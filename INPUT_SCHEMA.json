{
    "title": "YouTube Channel Scraper input",
    "description": "This actor takes channels as an input and returns their details.",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "input": {
            "title": "input urls",
            "type": "array",
            "description": "URLs of YouTube Channels",
            "editor": "json"
        }
    },
    "required": [
        "input"
    ]
}